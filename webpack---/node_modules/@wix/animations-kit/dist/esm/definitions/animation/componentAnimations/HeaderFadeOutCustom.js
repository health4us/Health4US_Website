import {
    __rest
} from "tslib";
const name = 'HeaderFadeOutCustom';
const properties = {
    schema: {
        duration: {
            type: 'number',
            min: 0,
            default: 0,
        },
        delay: {
            type: 'number',
            min: 0,
            default: 0,
        },
    },
};

function animate(kit, elements, duration, delay, _a) {
    var {
        ease = 'Quart.easeIn'
    } = _a, params = __rest(_a, ["ease"]);
    const sequence = kit.sequence(params);
    const animationParams = {
        ease,
        to: {
            autoAlpha: 0
        },
    };
    sequence.add(kit.animate('BaseFade', elements, duration, delay, animationParams));
    return sequence.get();
}
export {
    name,
    properties,
    animate
};
//# sourceMappingURL=HeaderFadeOutCustom.js.map