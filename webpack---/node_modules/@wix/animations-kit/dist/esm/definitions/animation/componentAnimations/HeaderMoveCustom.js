import {
    __rest
} from "tslib";
const name = 'HeaderMoveCustom';
const EASE_DEFAULT = 'Linear.easeNone';
const properties = {
    schema: {
        duration: {
            type: 'number',
            min: 0,
            default: 0,
        },
        delay: {
            type: 'number',
            min: 0,
            default: 0,
        },
    },
};

function animate(kit, elements, duration, delay, _a) {
    var {
        to = {}, ease = EASE_DEFAULT
    } = _a, params = __rest(_a, ["to", "ease"]);
    const sequence = kit.sequence(params);
    sequence.add(kit.animate('BasePosition', elements, duration, delay, {
        to,
        ease
    }));
    return sequence.get();
}
export {
    name,
    properties,
    animate
};
//# sourceMappingURL=HeaderMoveCustom.js.map