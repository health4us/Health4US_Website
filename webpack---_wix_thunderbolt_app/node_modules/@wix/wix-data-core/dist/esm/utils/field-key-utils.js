export function hasField(object, fieldNamePredicate) {
    if (!object) {
        return false;
    }
    if (Array.isArray(object)) {
        return object.some(function(ele) {
            return hasField(ele, fieldNamePredicate);
        });
    }
    if (typeof object === 'object') {
        var keys = Object.keys(object);
        return keys.some(function(k) {
            return fieldNamePredicate(k) || hasField(object[k], fieldNamePredicate);
        });
    }
    return false;
}
//# sourceMappingURL=field-key-utils.js.map